# Copie para .env na raiz do repo: cp .env.example .env
# STAGE=TEST usa mocks + Dynamo Local (valores hardcoded em Environments)

STAGE=TEST
MSS_NAME=clean_mss_template

# --- Opcional: só necessários quando STAGE != TEST (DEV / HOMOLOG / PROD) ---
# REGION=sa-east-1
# DYNAMO_TABLE_NAME=TemplateTable1-dev
# DYNAMO_PARTITION_KEY=pk
# DYNAMO_SORT_KEY=sk
# DYNAMO_ENDPOINT_URL=http://localhost:8000
# S3_TEMPLATE_BUCKET1_NAME=template_bucket1_name
# GRAPH_MICROSOFT_ENDPOINT=https://graph.microsoft.com/v1.0/me
